spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        generate_statistics: true
  flyway:
    enabled: true
  batch:
    job:
      enabled: false

logging:
  level:
    root: warn
    com.mopl: info

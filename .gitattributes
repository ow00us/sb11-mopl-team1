# 기본: 텍스트 파일은 자동 감지 + 저장소에는 LF로 통일 (OS 혼재 시 CRLF churn 방지)
* text=auto eol=lf

# 셸 스크립트는 LF 유지 (Linux/Mac/CI에서 실행)
gradlew text eol=lf

# Windows 배치 파일은 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 바이너리는 텍스트 처리 제외
*.jar binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
